import { makeStyles } from '@material-ui/core';

export const changePasswordPageStyle = makeStyles((theme) => ({
    root: {
        background: `linear-gradient(${theme.palette.primary.main}, ${theme.palette.primary.light})`,
    },
}));
