import { makeStyles } from '@material-ui/core';

export const wrapperStyle = makeStyles((theme) => ({
    root: {
        flex: 1,
    },
    container: {
        flex: 1,
        minHeight: '100%',
    },
}));
