import React from 'react';
import { observer } from 'mobx-react';
import { Grid } from '@material-ui/core';

export const RegisterPage: React.FC = observer(() => {
    return <Grid>Register Page</Grid>;
});
