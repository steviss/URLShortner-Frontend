import React from 'react';
import { observer } from 'mobx-react';
import { Grid } from '@material-ui/core';

export const PrivacyPage: React.FC = observer(() => {
    return <Grid>Privacy Page</Grid>;
});
