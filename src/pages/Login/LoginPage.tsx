import React from 'react';
import { observer } from 'mobx-react';
import { Grid } from '@material-ui/core';

export const LoginPage: React.FC = observer(() => {
    return <Grid>Login Page</Grid>;
});
